import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mi.primera.aplicacion',
  appName: 'mi-primera-aplicacion',
  webDir: 'dist'
};

export default config;
